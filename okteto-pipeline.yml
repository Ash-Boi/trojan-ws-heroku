build:
  frontend:
    context: frontend
    dockerfile: Dockerfile
    target: dev

deploy:
  commands:
    - okteto deploy --build -f docker-compose.yml
  endpoints:
    - path: /
      service: frontend
      port: 80    
